Reformas Integrales Amedida — Adenda de decisiones N0
Sesión: cierre de endpoints FastAPI, excepciones, revisión técnica final
TFM Máster en Agentes de IA e Hiperautomatización (EBIS)

Este documento complementa a Informe_Decisiones_N0_TFM_Reformas.txt. No lo
sustituye: añade las decisiones cerradas en la sesión de contrato de
endpoints y revisión técnica final.

1. Contrato final de endpoints FastAPI (patrón híbrido REST + RPC)

| # | Endpoint | Input | Output | Trigger real |
|---|---|---|---|---|
| 1 | POST /leads | nombre, email (se guarda entero en minúsculas), telefono (se normaliza; ver 5.1), tipo_reforma, m2, nivel_acabados, incluye_cambios_estructurales (m2: más de 0 y hasta 500, ver 5.1), fotos[] (opcional, máx. 5), lead_token (máx. 100 caracteres; clave de idempotencia, ver 5.1) | 201 (alta nueva) o 200 (repetición con el mismo lead_token): lead_id, cliente_id, oportunidad_id, status, creado (ver 5.1) | Webhook n8n, al confirmar el cliente los datos en el chat web (Agente 1) |
| 2 | POST /calculate-estimate | oportunidad_id | 200: presupuesto_id, oportunidad_id, importe_min_con_iva, importe_max_con_iva, motivo_gate, requiere_aprobacion, status, creado (ver 5.2 y 1.2) | Producción: MCP tool desde AI Agent. Testing/admin: invocación directa del endpoint, fuera del flujo n8n |
| 3 | POST /gate-decisions | presupuesto_id, decision, operador_id | presupuesto_id, nuevo_status | Botones inline de Telegram (aprobar/rechazar), reanuda nodo Wait de n8n |
| 4 | POST /visits | oportunidad_id, fecha_propuesta, franja_horaria | visita_id, status | Cliente elige franja tras recibir presupuesto (paso separado, no la misma llamada síncrona del presupuesto) |
| 5 | POST /create-followup-task | oportunidad_id, motivo | log_id, status | **[Actualizado por D13, 2026-09-20]** Producción N0: Schedule Trigger periódico de n8n (no un temporizador por lead) que consulta `oportunidades` con `estado='presupuesto_enviado' AND fecha_ultimo_contacto IS NULL` y más de 48h desde la creación, y llama a este endpoint por cada fila que cumple la condición. No pasa por MCP. MCP tool disponible para N1+, no ejercida en N0. Backend (columna `fecha_ultimo_contacto`, estado `seguimiento_pendiente`) implementado y verificado (17/17); el workflow de n8n en sí, no construido todavía. Ver `claude/Decisiones_D13_Seguimiento_48h_Barrido_Periodico.md` |
| 6 | GET /leads/session/{lead_token} | lead_token en la ruta (1 a 100 caracteres, mismas reglas que POST /leads) | 200 siempre que la petición sea válida: existe, lead_id, oportunidad_id, estado_oportunidad, presupuesto {existe, requiere_aprobacion, motivo_gate}. NUNCA importes (ver 5.3) | Router de n8n, al inicio de cada turno del chat: decide si el mensaje va al Agente 1 (captura) o al Agente 2 (asesor). Solo REST |

Requisito de autenticación de POST /leads (añadido en la sesión de
autenticación del webhook): además del cuerpo, toda petición debe traer la
cabecera HTTP X-Webhook-Secret con el valor de la variable de entorno
WEBHOOK_SECRET (secreto compartido con n8n, credencial "Header Auth").
Sin ella, o con un valor incorrecto, responde 401 antes de validar el
cuerpo. Ver 5.1.

get_business_rules() y request_missing_information() son MCP tools puras,
sin endpoint HTTP propio — leen directamente de services/.

Aclaración de diseño (resuelve ambigüedad de "doble invocación"): cada
endpoint con dualidad MCP/REST expone la MISMA función de services/ por
dos puertas de entrada distintas, nunca invocadas dos veces para el mismo
evento. En producción, cada evento real usa una sola puerta:
calculate-estimate -> MCP (agente); create-followup-task -> REST (timer).
La puerta alternativa existe para testing, uso administrativo manual o
capacidades futuras de N1/N2, no se ejerce en el flujo real de N0.

1.1 calculate-estimate — contrato revisado tras verificación de mercado
(2026-09-19)

Tres correcciones sobre la fila de calculate-estimate en la tabla
anterior, cerradas tras verificar tarifas_base y margen_empresa_pct
contra fuentes de mercado real 2026. Detalle numérico completo (bandas
de mercado por tipo de reforma, cálculo del doble cómputo de margen,
recalculo del umbral del Gate por m²): documento del proyecto TFM
claude/Decisiones_Verificacion_Tarifas_y_Margen_N0.md.

(a) INPUT: oportunidad_id — no lead_id, no los cuatro datos de negocio.
El endpoint NUNCA recibe tipo_reforma, nivel_acabados, m2 ni
incluye_cambios_estructurales como parámetros: los lee directamente de
leads.datos_estructurados / oportunidades.tipo_reforma a partir del
identificador. Motivo: por la puerta MCP el llamador es un LLM, y un
dato de negocio transcrito o alucinado por el agente no debe poder
alterar el cálculo de un precio que se le muestra a un cliente real. La
única forma de hacer eso estructuralmente imposible — no solo
detectable después — es que el agente no tenga ocasión de proporcionar
esos datos: invoca la tool y recibe el rango final ya calculado: no
interviene en ningún paso del cálculo, no ve tarifas_base ni
reglas_negocio.

Se usa oportunidad_id y no lead_id porque es la FK real de la que
cuelga presupuestos (presupuestos.oportunidad_id), y porque
oportunidades.lead_id no tiene UNIQUE (ver TFM_Decisiones_Modelo_Datos_
Leads.txt, D7): un lead duplicado por un reintento de n8n podría tener
dos oportunidades, y lead_id no identificaría cuál de las dos usar.

(b) OUTPUT: importe_min e importe_max sustituyen a importe_estimado.
presupuestos.importe_min/importe_max ya existían en el esquema sin
fórmula definida; la fórmula queda cerrada así:

    importe_min = (tarifas_base.precio_m2 × m2)
                  + recargo_informe_tecnico (si incluye_cambios_estructurales)
    importe_max = importe_min × (1 + margen_empresa_pct / 100)

margen_empresa_pct deja de aplicarse como recargo adicional sobre un
coste, que era la lectura original de esta Adenda. tarifas_base.precio_m2
son precios de venta de mercado ya verificados (no costes internos de
la empresa), y ya incorporan un margen implícito en qué extremo de la
banda de mercado se eligió al poblar cada fila (extremo competitivo vs.
extremo de margen). Aplicar margen_empresa_pct como multiplicador
adicional sobre ese precio duplicaba el margen y sacaba la oferta fuera
de mercado. El parámetro pasa a definir el ancho del rango de
negociación que se muestra al cliente (un techo por si aparecen
complejidades), no un recargo oculto sobre el coste.

presupuestos.duracion_estimada_dias se queda NULL en N0: no hay
fórmula definida para calcularla y no se ha pedido una. Limitación
documentada explícitamente, no una omisión silenciosa.
presupuestos.aprobado_por también queda NULL — lo escribe
POST /gate-decisions, no calculate-estimate.

(c) GATE evaluado siempre contra importe_max, nunca contra importe_min
ni una media: es la lectura conservadora (el peor caso de exposición
económica), coherente con el propósito del Gate de atrapar riesgo antes
de comprometerse. La combinación de motivo_gate sigue siendo la del
CHECK real de presupuestos.motivo_gate ('cambios_estructurales',
'importe_superior_umbral', 'ambos'):

    incluye_cambios_estructurales=true  y importe_max > umbral → 'ambos'
    incluye_cambios_estructurales=true  y importe_max ≤ umbral → 'cambios_estructurales'
    incluye_cambios_estructurales=false y importe_max > umbral → 'importe_superior_umbral'
    incluye_cambios_estructurales=false y importe_max ≤ umbral → sin Gate

El efecto en oportunidades.estado (transición 'presupuesto_enviado' vs.
un estado de espera del Gate) se cierra y documenta en
TFM_Decisiones_Modelo_Datos_Leads.txt, no aquí: esta Adenda fija el
contrato del endpoint, no el modelo de datos.

1.2 calculate-estimate — el precio se devuelve con IVA incluido (D14,
2026-09-20)

Corrección de un defecto de ambigüedad del contrato: los campos de salida
se llamaban importe_min e importe_max, y ese nombre no decía si la cifra
llevaba IVA. Quien integrara el endpoint tenía que adivinarlo, y adivinar
mal significa enseñarle al cliente un precio un 21 % por debajo del real.

Pasan a llamarse importe_min_con_iva e importe_max_con_iva, y contienen
el precio final, con el IVA ya aplicado: el número que se le puede
comunicar a un particular tal cual. La fórmula completa queda:

    importe_min_sin_iva = (tarifas_base.precio_m2 × m2)
                          + recargo_informe_tecnico (si aplica)
    importe_max_sin_iva = importe_min_sin_iva × (1 + margen_empresa_pct / 100)
    importe_min_con_iva = importe_min_sin_iva × (1 + iva_estandar_pct / 100)
    importe_max_con_iva = importe_max_sin_iva × (1 + iva_estandar_pct / 100)

iva_estandar_pct vive en reglas_negocio (valor 21) y se lee en cada
cálculo nuevo, nunca escrito en el código. Se usa el tipo general del
21 % y no el reducido del 10 % porque la elegibilidad del reducido
depende de condiciones sobre el cliente y el inmueble que no son
determinables en la fase de pre-estimación.

El GATE se sigue evaluando contra importe_max_SIN_iva (ver 1.1c): el
umbral mide riesgo comercial, y el IVA no se queda en la empresa. Queda
abierta la pregunta de si debería compararse contra el importe con IVA,
que es el que ve el cliente; cambiarlo obligaría a recalibrar los cuatro
umbrales de D9, así que se decide aparte.

Lo que se GUARDA en presupuestos es el importe ya con IVA
(importe_min_con_iva / importe_max_con_iva, columnas renombradas) junto
con iva_pct_aplicado, el tipo usado. Motivo: un presupuesto es un
documento comercial y debe quedar fijo en el momento en que se calculó,
no cambiar de precio si algún día se edita la regla. Por eso la rama que
atiende una llamada repetida devuelve lo guardado sin recalcular nada.

2. Motivos de create_followup_task (dos disparadores)

- sin_respuesta_visita: 48h desde la pre-estimación sin reservar visita.
  **[Mecanismo cerrado por D13, 2026-09-20]** Detectado por un Schedule
  Trigger periódico en n8n (no un nodo Wait por lead) que consulta
  `oportunidades` filtrando por `estado='presupuesto_enviado' AND
  fecha_ultimo_contacto IS NULL` y una antigüedad de creación superior a
  48h; cada fila encontrada dispara una llamada a este endpoint. Se
  prefirió este diseño porque el umbral de 48h se relee en cada ciclo
  desde `reglas_negocio` (una ejecución en espera no lo actualizaría
  retroactivamente) y el estado es auditable con una consulta SQL
  directa. La columna `fecha_ultimo_contacto` y el estado
  `seguimiento_pendiente` ya existen en el esquema (migración paso7,
  verificada, 17/17); el Schedule Trigger en sí no está construido
  todavía. Ver
  `claude/Decisiones_D13_Seguimiento_48h_Barrido_Periodico.md`.
- sin_decision_post_visita: N días desde visitas.fecha_propuesta sin que
  oportunidades.estado llegue a ganada/perdida. N = 5 días
  laborables, almacenado en reglas_negocio como
  dias_espera_decision_post_visita (confirmado e insertado en Supabase).

Ambos casos registran un log en la tabla logs (polimórfica) y generan una
notificación no bloqueante a administración/atención al cliente para
seguimiento telefónico. No se crea tabla de tareas nueva.

3. Gate HITL — notificación y reanudación (nodo Wait de n8n)

Canal de notificación a administración: Telegram (nodo nativo n8n),
elegido por velocidad de respuesta, cero coste de RAM adicional y soporte
de botones inline de aprobar/rechazar. La respuesta al CLIENTE ya se
resuelve de forma síncrona en el mismo webhook inicial (mensaje
diferenciado según motivo_gate, decisión ya cerrada en sesión anterior) —
la notificación Telegram es exclusivamente el aviso interno a
administración, no un segundo mensaje al cliente.

Flujo de reanudación: nodo Wait de n8n permanece a la espera de un
webhook disparado por los botones inline del mensaje de Telegram, que
invocan POST /gate-decisions directamente. Al recibir la decisión, el
flujo continúa (oferta de agendar cita si se aprueba).

4. Corrección: reserva de visita no es parte de la respuesta síncrona inicial

"Respuesta directa" (sin gate) y la respuesta tras aprobación del Gate
pasan a ser: presupuesto + OPCIÓN de agendar cita, no una reserva
automática. La reserva real (POST /visits) ocurre en una interacción
posterior separada, cuando el cliente elige franja horaria.

5. Tabla de excepciones y reintentos por endpoint (confirmada)

Política diferenciada: errores de validación (4xx) fallan rápido sin
reintentar; errores transitorios (5xx, timeout) se reintentan con
backoff. Detalle completo de reintentos, rutas de escalado y roles
notificados por endpoint: ver mensaje de sesión "Confirmamos. Haz una
revisión final..." — pendiente de trasladar al PDD junto con este
documento.

5.1 POST /leads — autenticación del webhook (añadido)

NOTA: esta adenda no contiene todavía la tabla completa de excepciones
por endpoint (sigue en el mensaje de sesión citado arriba, pendiente de
trasladar al PDD). Esta subsección recoge solo la fila de la
autenticación, añadida en el mismo bloque de trabajo que el código para
que el contrato no quede desactualizado. Las demás filas de POST /leads
se completarán al trasladar la tabla.

| Endpoint | Código | Causa | Reintento en n8n | Acción |
|---|---|---|---|---|
| POST /leads | 401 No autorizado | Cabecera X-Webhook-Secret ausente, vacía o con valor distinto de WEBHOOK_SECRET | NO (4xx: fallo rápido; reintentar con el mismo secreto daría siempre 401) | Revisar la credencial "Header Auth" del nodo HTTP de n8n frente al .env del backend. No se escribe nada en la base de datos (ni en logs) |
| POST /leads | 422 Dato inválido | m2 fuera de rango: 0, negativo o mayor que 500 (añadido el 2026-09-20). El error nombra el campo: "Input should be less than or equal to 500" / "Input should be greater than 0" | NO (4xx: el mismo cuerpo daría siempre 422) | Corregir el dato en el formulario. No se escribe nada en la base de datos. Defensa doble: si el dato entrara por otra vía (script, editor SQL de Supabase, nodo Postgres de n8n), lo rechaza el CHECK chk_leads_m2_rango |
| POST /leads | 422 Dato inválido | telefono con forma inválida (añadido el 2026-09-24). Tras quitar espacios (incluidos salto de línea y espacio duro), guiones, puntos y paréntesis debe quedar un "+" opcional y de 9 a 15 dígitos [0-9]. Ejemplo rechazado: "telefono666555". El error nombra el campo: loc ["body","telefono"], "teléfono no válido: ..." | NO (4xx: el mismo cuerpo daría siempre 422) | El Agente 1 pide solo la corrección de ese campo. No se escribe nada en la base de datos. Si es válido, se guarda NORMALIZADO ("+34 666-777-444" -> "+34666777444") |
| POST /leads | 422 Dato inválido | lead_token de más de 100 caracteres (añadido el 2026-09-24): "String should have at most 100 characters" | NO | Revisar cómo genera n8n el token. Replica el ancho de leads.lead_token VARCHAR(100) |
| POST /leads | 200 con creado = false | Repetición: ya existe un lead con ese lead_token (añadido el 2026-09-24) | No aplica (no es un error) | Se devuelven lead_id, cliente_id, oportunidad_id y el estado ACTUAL de la oportunidad del lead original. No se escribe nada. Ver "Idempotencia" más abajo |

Idempotencia de POST /leads (añadido el 2026-09-24, migración paso8).
lead_token se guarda en leads.lead_token, con UNIQUE (leads_lead_token_key).
Si n8n reintenta con el mismo token, la inserción del lead choca con esa
UNIQUE, toda la transacción se deshace (incluido el upsert del cliente) y
se devuelve el lead existente con creado = false. Lo resuelve la base de
datos, no un SELECT previo, así que dos peticiones simultáneas con el
mismo token no pueden crear dos leads (verificado con 8 llamadas
simultáneas: un solo lead, un solo creado = true). Código HTTP: 201 en
un alta nueva y 200 en una repetición, el mismo código que devuelve
POST /calculate-estimate en su repetición (criterio: coherencia entre
endpoints de la misma API; P8 de Plan_calculate_estimate.txt, "un 201
mentiría en los reintentos"). A diferencia de calculate-estimate, que
responde 200 siempre por ser una llamada de tipo "calcula", POST /leads
sí crea un recurso, así que un alta real conserva el 201. n8n trata
cualquier 2xx como éxito y lee creado del cuerpo. Si una repetición trae datos distintos con el mismo token,
se devuelve el lead original y los datos nuevos se ignoran. Los leads
anteriores a paso8 tienen lead_token NULL. Verificación:
scripts/check_create_lead_service.py y scripts/check_leads_endpoint_http.py.

Email sin distinguir mayúsculas (añadido el 2026-09-24, migración
paso8). LeadCreate pasa el email entero a minúsculas (EmailStr solo baja
el dominio), y clientes tiene un índice único sobre lower(email)
(clientes_email_lower_key) en lugar de la antigua UNIQUE (email):
"Pepe@Gmail.com" y "pepe@gmail.com" son el mismo cliente.

Contacto de cada llamada (añadido el 2026-09-24). leads.datos_estructurados
guarda, bajo la clave "contacto", el nombre, email y teléfono de ESA
llamada. La ficha de clientes no se sobrescribe (D2), así que con un email
repetido el contacto nuevo queda en su lead.

Detalles del 401: la respuesta es idéntica haya faltado la cabecera o
sea incorrecta (cuerpo {"detail": "No autorizado"} y cabecera
WWW-Authenticate: APIKey), para no dar pistas a quien pruebe valores. El
secreto se compara en tiempo constante (secrets.compare_digest).
Implementación: app/api/security.py. Verificación:
scripts/check_webhook_auth_http.py.

5.2 POST /calculate-estimate y tool MCP calculate_estimate (añadido el
2026-09-19, en el mismo bloque que el código)

Autenticación por puerta:
  - REST: la misma cabecera X-Webhook-Secret que POST /leads (misma
    dependencia, app/api/security.py). Es obligatoria porque esta puerta
    devuelve siempre los importes y escribe en la base de datos.
  - MCP: cabecera "Authorization: Bearer <MCP_SECRET>", un secreto
    DISTINTO de WEBHOOK_SECRET (app/mcp_server/server.py). Sin él, o con
    otro valor (incluido WEBHOOK_SECRET), /mcp responde 401 con
    WWW-Authenticate: Bearer.

| Puerta | Código / resultado | Causa | Reintento en n8n | Acción |
|---|---|---|---|---|
| REST | 200 | Presupuesto calculado ahora (creado=true) o ya existente (creado=false) | — | n8n solo envía el aviso a Telegram si creado=true y requiere_aprobacion=true |
| REST | 200 con status="requiere_revision" | Falta la tarifa, una regla de negocio o un dato del lead | NO (reintentar no lo arregla) | Se registra en logs (accion presupuesto_requiere_revision, con lo que faltaba); revisión humana |
| REST | 401 | Falta X-Webhook-Secret o es incorrecto | NO | Revisar la credencial Header Auth |
| REST | 404 | La oportunidad no existe | NO | Revisar el oportunidad_id que envía n8n |
| REST | 409 | La oportunidad no tiene presupuesto y no está en 'nueva' | NO | Revisión humana del estado de la oportunidad |
| REST | 422 | Cuerpo inválido: falta oportunidad_id, no es entero > 0, o trae campos de más (m2, lead_id...) | NO | Corregir la petición |
| REST | 500 | Error no controlado (p. ej. Postgres caído) | SÍ, con backoff | Registrado por el manejador global |
| MCP | resultado normal | Igual que el 200 REST, salvo que importe_min/importe_max van a null si motivo_gate es cambios_estructurales o ambos | — | El agente no debe estimar ni inventar importes |
| MCP | ToolError | Oportunidad inexistente, estado no permitido, o argumentos inválidos/extra | NO | El agente informa de que no se pudo calcular |
| MCP | 401 | Token Bearer ausente o incorrecto | NO | Revisar la credencial del nodo MCP Client Tool |

Idempotencia: llamar dos veces para la misma oportunidad devuelve el
mismo presupuesto_id, sin recalcular ni escribir nada (UNIQUE en
presupuestos.oportunidad_id, D8). Los importes viajan en el JSON como
TEXTO exacto ("8625.00"), no como número.

Implementación: app/services/estimate_service.py, app/api/estimates.py,
app/mcp_server/server.py. Verificación: scripts/check_estimate_service.py,
scripts/check_calculate_estimate_http.py y
scripts/check_mcp_calculate_estimate.py.

Ejemplo de fallback en FastAPI (calculate_estimate): si la combinación
tipo_reforma/nivel_acabados no existe en tarifas_base, no se inventa un
precio — se registra la incidencia en logs y se devuelve
status="requiere_revision", importe_min=None, importe_max=None.

Ejemplo de manejo de excepción en n8n: Error Trigger workflow separado,
capturado tras agotar reintentos, que (1) registra en logs vía FastAPI,
(2) notifica al rol correspondiente según la tabla de excepciones, (3) si
el fallo ocurre antes de responder al webhook síncrono, devuelve un
mensaje de fallback controlado al cliente en vez de dejar la conexión
sin respuesta.

5.3 GET /leads/session/{lead_token} (añadido el 2026-09-25, en el mismo
bloque que el código)

Para qué: el router de n8n lo llama al inicio de cada turno del chat para
decidir si el mensaje va al Agente 1 (captura: todavía no hay lead) o al
Agente 2 (asesor: el lead ya existe). Solo REST; no hay tool MCP, porque
quien lo usa es el router, no un agente.

Autenticación: la misma cabecera X-Webhook-Secret que POST /leads (misma
dependencia, app/api/security.py). La autenticación va ANTES que la
validación de la ruta: un token de 101 caracteres sin cabecera da 401, no
422 (verificado).

Respuesta (200):
  {
    "existe": bool,
    "lead_id": int | null,
    "oportunidad_id": int | null,
    "estado_oportunidad": str | null,
    "presupuesto": {"existe": bool,
                    "requiere_aprobacion": bool | null,
                    "motivo_gate": str | null}
  }
  - presupuesto es siempre un objeto, nunca null.
  - requiere_aprobacion vale null si no hay presupuesto; false significa
    "hay presupuesto y no tiene Gate".
  - motivo_gate: 'cambios_estructurales', 'importe_superior_umbral',
    'ambos' o null.

| Código / resultado | Causa | Reintento en n8n | Acción |
|---|---|---|---|
| 200 con existe=true | Hay un lead con ese lead_token | — | El router envía el mensaje al Agente 2 |
| 200 con existe=false | No hay ningún lead con ese token (todos los demás campos null, presupuesto {false, null, null}) | — | No es un error: el router envía el mensaje al Agente 1 |
| 401 | Falta X-Webhook-Secret o es incorrecto | NO | Revisar la credencial Header Auth |
| 422 | lead_token de más de 100 caracteres: loc ["path","lead_token"], "String should have at most 100 characters" | NO | Revisar cómo genera n8n el token |
| 500 | Error no controlado (p. ej. Postgres caído) | SÍ, con backoff | Registrado por el manejador global |

NUNCA devuelve importes (D18.5: el Agente 2 no debe tenerlos). Defensa
doble: la consulta no selecciona importe_min_con_iva ni
importe_max_con_iva, y el esquema de respuesta (LeadSessionResponse) no
tiene ningún campo de importe; FastAPI construye el JSON solo con los
campos del response_model. Prueba en negativo: una versión que lee los
importes y los devuelve desde services/ sigue sin sacarlos (24/24); solo
cuando además se añaden al esquema aparecen en el JSON, y la
comprobación falla.

Varias oportunidades para un mismo lead (oportunidades.lead_id no tiene
UNIQUE): se devuelve la PRIMERA (ORDER BY o.id), el mismo criterio que la
idempotencia de POST /leads, y el servidor deja un [AVISO] en stderr. La
respuesta no cambia.

Coste: una consulta (leads LEFT JOIN oportunidades LEFT JOIN presupuestos,
por el índice único de lead_token) más la validación de la conexión del
pool. Se paga en cada turno del chat.

Implementación: app/services/leads_service.py (get_lead_session),
app/api/leads.py, app/schemas/leads.py (LeadSessionResponse,
PresupuestoSesion). Límites de lead_token: MIN_LEAD_TOKEN y
MAX_LEAD_TOKEN en app/schemas/common.py, compartidos con LeadCreate.
Verificación: scripts/check_lead_session_http.py.

6. Revisión técnica final — hallazgos y resolución

- RLS habilitado en reglas_negocio y tarifas_base (RESUELTO en sesión,
  ALTER TABLE ... ENABLE ROW LEVEL SECURITY ejecutado).
- Ambigüedad MCP/REST en calculate-estimate y create-followup-task
  (RESUELTO, ver punto 1).
- Falta de mecanismo de reanudación tras Gate HITL (RESUELTO, ver
  punto 3, nodo Wait + Telegram).
- Reserva de visita incorrectamente descrita como parte de la respuesta
  síncrona inicial (RESUELTO, ver punto 4).
- Contrato de calculate-estimate con doble fuente de verdad para los
  datos de negocio (input duplicaba lo ya guardado en leads/oportunidades)
  y margen_empresa_pct aplicado como recargo sobre un precio que ya era
  de venta (RESUELTO, ver punto 1.1).
- Session pooler de Supabase bajo concurrencia alta con webhook síncrono:
  LIMITACIÓN DOCUMENTADA, no resuelta — a revisar si el tiempo lo
  permite. Anotar en el apartado de limitaciones de la memoria.
- Presión de RAM (8GB) con n8n + FastAPI + VS Code + Claude Code
  simultáneos: nota operativa para el día de grabación del vídeo-demo,
  no requiere cambio de arquitectura.

7. Pendiente para próxima sesión

- Trasladar el contrato de endpoints y la tabla de excepciones al PDD.
- Diseñar el servidor MCP en Python (estructura compartida con FastAPI
  vía services/).
- Explorar opciones para el riesgo de concurrencia del Session pooler
  (punto 5 de esta adenda).
- HECHO (2026-09-19): 'pendiente_aprobacion' añadido al esquema real
  (migración paso3), a schema_actual.sql y a
  TFM_Decisiones_Modelo_Datos_Leads.txt como D8.

8. Correcciones de datos (2026-09-19)

Datos de esta adenda que contradecían la base de datos real o el código
ya implementado. Se corrigen en su sitio; aquí queda constancia de qué
decía antes y de dónde sale el valor correcto.

Fuente: docs/schema_actual.sql (generado desde la base de datos real)

- Punto 2: "visitas.fecha" -> visitas.fecha_propuesta. La tabla visitas
  no tiene ninguna columna llamada "fecha".
- Punto 2: "convertido/descartado" -> ganada/perdida. Esos dos valores
  no existen en el CHECK oportunidades_estado_check; un UPDATE a
  'convertido' fallaría. (Misma equivalencia ya documentada en
  TFM_Decisiones_Modelo_Datos_Leads.txt, "CORRECCIÓN DE NOMENCLATURA".)

Fuente: código de POST /leads (app/schemas/leads.py, app/api/leads.py)

- Fila 1, input: se añaden lead_token (obligatorio en LeadCreate) y la
  aclaración de que fotos[] es opcional con un máximo de 5
  (MAX_FOTOS_LEAD).
- Fila 1, output: se añade oportunidad_id (ampliación decidida en D1,
  TFM_Decisiones_Modelo_Datos_Leads.txt) y el código de éxito 201.

Detectado pero NO corregido aquí (pendiente de decisión):

- Fila 4 (POST /visits): el input "franja_horaria" no tiene columna en
  visitas. Se decidirá al diseñar ese endpoint.
- La descripción de la regla dias_espera_decision_post_visita, guardada
  en la propia tabla reglas_negocio de Supabase, repite "visitas.fecha"
  y "convertido/descartado". Es un dato de la base de datos, no de este
  documento: corregirlo requiere un UPDATE sobre reglas_negocio.
- Punto 1.1: remite a claude/Decisiones_Verificacion_Tarifas_y_Margen_N0.md,
  que no está en este repositorio.
- RESUELTO (2026-09-19). Punto 1.1(a): nombra dos fuentes para
  tipo_reforma. Manda la columna oportunidades.tipo_reforma (el dato de
  trabajo según D3). nivel_acabados, m2 e incluye_cambios_estructurales
  salen de leads.datos_estructurados, porque no existen en otro sitio.